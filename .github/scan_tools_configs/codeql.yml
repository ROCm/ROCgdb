paths-ignore:
  # TODO: Temporary workaround for a CodeQL c-cpp extraction failure where
  # runtime-core.cpp causes the PR security scan job to be terminated during
  # "Perform CodeQL Analysis". Remove after upstream CodeQL extraction for
  # this test source is fixed and c-cpp scan completes without this exclusion.
  - gdb/testsuite/gdb.rocm/runtime-core.cpp
